FROM python:3.11-slim  
WORKDIR /app  
COPY requirements.txt .  
RUN pip install --no-cache-dir -r requirements.txt  
COPY server.py .  
# 关键：直接用 python -m uvicorn 启动，不依赖 mcp.app  
CMD ["python", "-m", "uvicorn", "server:mcp.app", "--host", "0.0.0.0", "--port", "8080"]  
